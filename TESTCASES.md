# 記帳本 Android — 測試規格 v1

> **本文件是可攜的測試契約，從 [expense-tracker](https://github.com/jojomango/expense-tracker)
> 網頁版的 `TESTCASES.md` 移植而來。**
> Domain 測案（T1–T7）與 E2E 行為規格（E2E-1～E2E-10）**與網頁版逐條對應**——
> 這些是已經被網頁版正式驗證過的業務規則，不是重新設計的。
> UI 元件測試（Compose UI Testing）不在本文件範圍，視為可拋棄的實作細節。

## 測試分層

| 層 | 位置 | 工具 | 對應網頁版 |
|---|---|---|---|
| Domain 單元 | `domain/src/test/` | JUnit 5 | `tests/domain/`（Vitest）邏輯原封移植 |
| Persistence | `data/src/test/` | JUnit 5 + Room in-memory DB | `tests/persistence/`（fake-indexeddb） |
| E2E 行為 | `.maestro/` | Maestro | `tests/e2e/`（Playwright），規格保留、腳本重寫 |
| UI 元件 | `ui/src/androidTest/` | Compose UI Testing | 拋棄，Android 版自己重新決定要不要寫 |

**跟網頁版的一個實作差異，先講在前面：** `kotlinx.datetime.LocalDate` 本身就是
「純曆日，不含時間與時區」的型別，不像 JavaScript 的 `Date` 會混入時區資訊。
所以網頁版 `TESTCASES.md` 裡那些「絕對不要用 `new Date(y,m,d)` 比較，否則 DST
會出錯」的警語在 Kotlin 這裡**不適用**——只要全程用 `LocalDate` 而不是
`Instant`／`LocalDateTime`，就天生沒有這個坑。下面的測案數值原封不動保留
（用來驗證日期運算邏輯本身是對的），但「為什麼要小心」的實作提示已經按 Kotlin
的情況改寫。

---

# T1 — Money（金額）

## T1.1 建構與驗證

| # | 條件 | 預期 |
|---|---|---|
| T1.1.1 | `Money.of(10050, "TWD")` | 內部值 `10050L`，顯示 `NT$100.50` |
| T1.1.2 | `Money.of(1000, "JPY")` | 顯示 `¥1,000`（JPY 0 位小數） |
| T1.1.3 | `Money.of(0, "TWD")` | 合法，顯示 `NT$0.00` |
| T1.1.4 | `Money.of(-500, "TWD")` | 合法（餘額可為負），顯示 `-NT$5.00` |
| T1.1.5 | 嘗試以非整數建構（例如把 `1.5` 轉型硬塞） | 型別系統層級應該就不允許——`amount` 型別是 `Long`，不接受小數，這題在 Kotlin 幾乎是免費的（編譯期擋掉，不需要 runtime 測試） |
| T1.1.6 | `Money.of(100, "XXX")`（未知幣別） | **拋 `IllegalArgumentException`** — 未知幣別代碼 |
| T1.1.7 | `Money.of(Long.MAX_VALUE / 2, "TWD")` | 合法，不溢位 |

## T1.2 解析使用者輸入

| # | 輸入（TWD） | 預期內部值 |
|---|---|---|
| T1.2.1 | `"100"` | `10000L` |
| T1.2.2 | `"100.5"` | `10050L` |
| T1.2.3 | `"100.50"` | `10050L` |
| T1.2.4 | `"100.567"` | **拋錯** — 超過該幣別小數位 |
| T1.2.5 | `"1,234.56"` | `123456L`（容許千分位） |
| T1.2.6 | `""` / `"abc"` / `"-"` | **拋錯** |
| T1.2.7 | `"0"` | `0L`（Money 合法，但交易金額驗證會另外拒絕） |
| T1.2.8 | 輸入 `"100.5"`（JPY，0 位小數） | **拋錯** |

## T1.3 運算

| # | 條件 | 預期 |
|---|---|---|
| T1.3.1 | `TWD(10050).plus(TWD(2550))` | `TWD(12600)` |
| T1.3.2 | `TWD(10000).minus(TWD(15000))` | `TWD(-5000)` |
| T1.3.3 | `TWD(100).plus(JPY(100))` | **拋錯** — 幣別不符，禁止跨幣運算 |
| T1.3.4 | `sum(emptyList())`（指定 TWD） | `TWD(0)` |
| T1.3.5 | 連續加 0.1 共 10 次（TWD，以最小單位整數運算） | 恰為 `TWD(100)` — **無浮點誤差**（Kotlin 用 `Long` 天生沒有這個問題，但仍要測，避免有人手滑在中途轉成 `Double`） |
| T1.3.6 | `TWD(10000).percentOf(TWD(30000))` | `33.33`（回傳 `Double`，僅供顯示） |
| T1.3.7 | `percentOf` 分母為 0 | 回傳 `0.0`，不得為 `NaN` / `Infinity` |

---

# T2 — Week（週計算）

**這是最容易出錯、也最需要覆蓋的區塊**——即使 `LocalDate` 天生沒有時區問題，
「週起始日往前/往後推算」這種曆法邊界計算還是很容易寫錯，值得完整覆蓋。

## T2.1 週區間（weekStartDay = MONDAY）

以 `2026-08-11`（週二）為基準：

| # | 輸入日期 | 星期 | 預期週區間 |
|---|---|---|---|
| T2.1.1 | `2026-08-11` | 二 | `2026-08-10` ~ `2026-08-16` |
| T2.1.2 | `2026-08-10` | 一 | `2026-08-10` ~ `2026-08-16`（**週首邊界**） |
| T2.1.3 | `2026-08-16` | 日 | `2026-08-10` ~ `2026-08-16`（**週尾邊界**） |
| T2.1.4 | `2026-08-09` | 日 | `2026-08-03` ~ `2026-08-09`（**前一週**） |
| T2.1.5 | `2026-08-17` | 一 | `2026-08-17` ~ `2026-08-23`（**下一週**） |

## T2.2 週區間（weekStartDay = SUNDAY）

| # | 輸入日期 | 星期 | 預期週區間 |
|---|---|---|---|
| T2.2.1 | `2026-08-11` | 二 | `2026-08-09` ~ `2026-08-15` |
| T2.2.2 | `2026-08-09` | 日 | `2026-08-09` ~ `2026-08-15`（週首） |
| T2.2.3 | `2026-08-15` | 六 | `2026-08-09` ~ `2026-08-15`（週尾） |
| T2.2.4 | `2026-08-16` | 日 | `2026-08-16` ~ `2026-08-22` |

## T2.3 週起始日全七種取值

| # | weekStartDay | 基準日 `2026-08-11`（二） | 預期週首 |
|---|---|---|---|
| T2.3.1 | SUNDAY | | `2026-08-09` |
| T2.3.2 | MONDAY | | `2026-08-10` |
| T2.3.3 | TUESDAY | | `2026-08-11`（當天即週首） |
| T2.3.4 | WEDNESDAY | | `2026-08-05` |
| T2.3.5 | THURSDAY | | `2026-08-06` |
| T2.3.6 | FRIDAY | | `2026-08-07` |
| T2.3.7 | SATURDAY | | `2026-08-08` |

## T2.4 跨界情境

| # | 條件 | 預期 |
|---|---|---|
| T2.4.1 | 跨月：`2026-07-30`（四），weekStart=MONDAY | `2026-07-27` ~ `2026-08-02` |
| T2.4.2 | 跨年：`2026-01-01`（四），weekStart=MONDAY | `2025-12-29` ~ `2026-01-04` |
| T2.4.3 | 閏年 2 月：`2028-02-29`（二），weekStart=MONDAY | `2028-02-28` ~ `2028-03-05` |
| T2.4.4 | 同一日期、不同 weekStartDay | 回傳不同區間，且**皆包含該日期** |
| T2.4.5 | 任意日期 × 任意 weekStartDay（property test，用 [kotest property-based testing](https://kotest.io/docs/proptest/property-based-testing.html) 或手刻 1000 次隨機迴圈皆可） | 區間長度恆為 7 天，且輸入日期恆落在區間內 |
| T2.4.6 | （網頁版原本測 DST／時區邊界，`LocalDate` 天生沒有這個問題，改測：）連續 400 天，每天都算一次週區間 | 每個區間長度恆為 7 天，不因為某天是 29/30/31 號而出錯 |

## T2.5 週分組

| # | 條件 | 預期 |
|---|---|---|
| T2.5.1 | 8 筆分屬 3 週的交易，weekStart=MONDAY | 分為 3 組，組內依日期排序，組間依週首倒序 |
| T2.5.2 | 空清單 | 回傳空 list，不拋錯 |
| T2.5.3 | 全部在同一天 | 1 組 |
| T2.5.4 | 相同資料、weekStartDay 由 MONDAY 改為 SUNDAY | 分組結果改變（驗證設定確實生效） |

---

# T3 — Budget（預算與餘額）

## T3.1 週預算（budgetMode = WEEKLY）

情境：日常錢包，TWD，週預算 3000.00（`300000L`），weekStart=MONDAY，今天 `2026-08-11`（二）

| # | 交易 | 預期餘額 |
|---|---|---|
| T3.1.1 | 無交易 | `300000`（NT$3,000.00） |
| T3.1.2 | 本週支出 500 + 300 | `300000 - 80000 = 220000` |
| T3.1.3 | 本週支出 3500 | `-50000`（**超支，負值**），`isOverBudget = true` |
| T3.1.4 | 本週支出 3000（剛好用完） | `0`，`isOverBudget = false`（**邊界：0 不算超支**） |
| T3.1.5 | 本週有一筆收入 5000 | 餘額**不受影響**（D1：只計支出） |
| T3.1.6 | 上週支出 2000，本週 0 | 本週餘額 `300000`（**不結轉、不累計**） |
| T3.1.7 | 交易日期為本週週首當天 | **計入** |
| T3.1.8 | 交易日期為本週週尾當天 | **計入** |
| T3.1.9 | 交易日期為週首前一天 | **不計入** |
| T3.1.10 | 交易日期為週尾後一天 | **不計入** |
| T3.1.11 | 未來日期（本週內）的交易 | **計入**（預先記帳） |
| T3.1.12 | 其他錢包的交易 | **不計入**（錢包隔離） |

## T3.2 總預算（budgetMode = TOTAL）

情境：日本旅遊錢包，JPY，總預算 200000

| # | 交易 | 預期 |
|---|---|---|
| T3.2.1 | 無交易 | 餘額 `200000`，已用 `0%` |
| T3.2.2 | 支出 50000 | 餘額 `150000`，已用 `25%` |
| T3.2.3 | 支出跨越多週（3 週共 180000） | 餘額 `20000`，**週界完全無關** |
| T3.2.4 | 支出 250000 | 餘額 `-50000`，已用 `125%`，`isOverBudget = true` |
| T3.2.5 | 有一筆收入 30000 | 餘額**不受影響**（D1） |
| T3.2.6 | 總預算為 0 | 已用百分比回傳 `0.0`，不得 `NaN` |

## T3.3 無預算（budgetMode = NONE）

| # | 條件 | 預期 |
|---|---|---|
| T3.3.1 | 呼叫餘額計算 | 回傳 `null`，UI 據此隱藏餘額卡片 |
| T3.3.2 | 呼叫本週支出總額 | 仍正常回傳數值 |

## T3.4 多錢包隔離（多幣別關鍵測案）

情境：日常錢包（TWD，週預算 3000）+ 旅遊錢包（JPY，總預算 200000），兩者皆有交易

| # | 條件 | 預期 |
|---|---|---|
| T3.4.1 | 計算日常錢包餘額 | 只含 TWD 交易，回傳 `Money(TWD)` |
| T3.4.2 | 計算旅遊錢包餘額 | 只含 JPY 交易，回傳 `Money(JPY)` |
| T3.4.3 | 任何 API 嘗試加總兩錢包 | **不存在此 API**（規格上不提供） |
| T3.4.4 | 兩錢包餘額計算互不影響 | 修改 JPY 交易後 TWD 餘額不變 |

## T3.5 分類彙總

| # | 條件 | 預期 |
|---|---|---|
| T3.5.1 | 5 筆分屬 3 分類的支出 | 3 組，依金額倒序，含各組佔比 |
| T3.5.2 | 混合收入與支出 | 支出與收入分開彙總，不混算 |
| T3.5.3 | 有交易的分類被刪除 | 該筆歸入「未分類」，交易不遺失 |
| T3.5.4 | 空清單 | 空 list，佔比計算不 crash |

## T3.6 近 N 週支出趨勢（`summarizeWeeklyTrend`）

情境同 T3.1（日常錢包，TWD，週預算 3000，weekStart=MONDAY，今天 `2026-08-11`）

| # | 條件 | 預期 |
|---|---|---|
| T3.6.1 | 呼叫 `summarizeWeeklyTrend(..., weeksCount=8)` | 回傳 8 筆，依週首由舊到新排序，最後一筆即為本週 |
| T3.6.2 | 本週支出 10000、上週支出 20000 | 各自只計入落在該週區間內的支出，不跨週混算 |
| T3.6.3 | 本週有一筆收入 50000 | 該週總額不受影響（延續 D1：只計 expense） |
| T3.6.4 | 其他錢包的交易 | **不計入**（錢包隔離） |
| T3.6.5 | `budgetMode = NONE` 的錢包 | 仍可正常計算（與 budgetMode 無關） |
| T3.6.6 | `weeksCount = 0` | 回傳空 list |

## T3.7 每週剩餘天數（`daysLeftInWeek`）

時間一律由參數注入，**不得**在函式內呼叫 `Clock.System.now()`。

| # | weekStartDay | referenceDate | 期望 |
|---|---|---|---|
| T3.7.1 | MONDAY | `2026-09-03`（週四） | 4 |
| T3.7.2 | MONDAY | `2026-09-06`（週日，該週最後一天） | 1 |
| T3.7.3 | MONDAY | `2026-08-31`（週一，該週第一天） | 7 |
| T3.7.4 | SUNDAY | `2026-09-06`（週日） | 7 |
| T3.7.5 | SUNDAY | `2026-09-05`（週六） | 1 |
| T3.7.6 | WEDNESDAY | `2026-09-03`（週四） | 6 |

## T3.8 日均可用額（`dailyAllowance`）

| # | remaining | daysLeft | 期望 |
|---|---|---|---|
| T3.8.1 | 1146（TWD 最小單位） | 4 | 286（向下取整，不四捨五入） |
| T3.8.2 | 0 | 4 | 0 |
| T3.8.3 | -500（已超支） | 4 | 0 |
| T3.8.4 | 1000 | 1 | 1000 |
| T3.8.5 | 1000（JPY，0 位小數） | 3 | 333 |
| T3.8.6 | 任意 | 0 | 拋 `IllegalArgumentException` |

---

# T4 — Persistence（持久層，Room）

| # | 條件 | 預期 |
|---|---|---|
| T4.1.1 | 新增交易後重新開啟 DB | 資料存在且欄位一致 |
| T4.1.2 | 刪除錢包 | 該錢包所有交易一併刪除，其他錢包不受影響 |
| T4.1.3 | 刪除最後一個錢包 | **被拒絕**，拋出明確錯誤 |
| T4.1.4 | 首次啟動 | 自動建立預設分類（7 支出 + 4 收入） |
| T4.1.5 | Room schema v1 → v2 migration | 既有資料完整保留（用 Room 的 `MigrationTestHelper` 寫骨架） |
| T4.1.6 | 併發寫入同一交易 | 後寫入者勝出，不產生髒資料 |
| T4.1.7 | 刪除系統預設分類 | **被拒絕**，拋出明確錯誤，分類與交易皆不受影響 |

## T4.2 匯出 / 匯入

| # | 條件 | 預期 |
|---|---|---|
| T4.2.1 | 匯出後立即匯入（replace） | 資料完全一致（round-trip 無損） |
| T4.2.2 | 匯入 schema version 較新的檔 | **拒絕**，提示需更新 app |
| T4.2.3 | 匯入缺少必要欄位的檔 | **整批拒絕**（原子性），現有資料不變 |
| T4.2.4 | 匯入格式錯誤的 JSON | 拋出可讀錯誤，不 crash |
| T4.2.5 | merge 模式，id 衝突 | 保留 `updatedAt` 較新者 |
| T4.2.6 | merge 模式，id 不衝突 | 兩邊資料皆保留 |
| T4.2.7 | 匯入 10,000 筆交易 | 3 秒內完成，UI 不凍結（用 `Dispatchers.IO` 跑，不要卡主執行緒） |
| T4.2.8 | 匯入引用不存在 categoryId 的交易 | **拒絕**（參照完整性檢查） |

---

# T5 — Month（月份計算）

供「本月」分類支出佔比使用。

## T5.1 月區間（`monthRangeOf`）

| # | 輸入日期 | 預期月區間 |
|---|---|---|
| T5.1.1 | `2026-08-11`（一般月份，31 天） | `2026-08-01` ~ `2026-08-31` |
| T5.1.2 | `2026-04-15`（小月，30 天） | `2026-04-01` ~ `2026-04-30` |
| T5.1.3 | `2026-02-15`（平年 2 月，28 天） | `2026-02-01` ~ `2026-02-28` |
| T5.1.4 | `2028-02-15`（閏年 2 月，29 天） | `2028-02-01` ~ `2028-02-29` |
| T5.1.5 | `2026-08-01`（月首當天） | 本身即為區間起點 |
| T5.1.6 | `2026-08-31`（月尾當天） | 本身即為區間終點 |
| T5.1.7 | `2026-12-25`（12 月） | `2026-12-01` ~ `2026-12-31`（跨年邊界不受影響） |

（網頁版原本有一條「非法格式輸入拋錯」的測案——Kotlin 用 `LocalDate` 當參數型別，
非法格式在呼叫端解析字串時就會被擋掉，函式本身不需要再測這件事，這是型別系統
幫你省下的一個測案。）

---

# T6 — Category（分類規則）

## T6.1 分類刪除規則

`SPEC.md` §3.3：系統預設分類可改名但不可刪除。

| # | 條件 | 預期 |
|---|---|---|
| T6.1.1 | `category.isDefault == true` | **拋出明確錯誤**（例如 `DefaultCategoryException`） |
| T6.1.2 | `category.isDefault == false` | 不拋錯 |

## T6.2 分類色驗證

| # | 情境 | 期望 |
|---|---|---|
| T6.2.1 | `color = "#C1502E"` | 通過 |
| T6.2.2 | `color = ""` | 拋錯 |
| T6.2.3 | `color = "red"` | 拋錯（只接受 `#rrggbb`） |
| T6.2.4 | `color = "#FFF"` | 拋錯（不接受三位縮寫） |
| T6.2.5 | 預設分類種子 | 11 個分類都有 color，且與 `UI-SPEC.md` §2.2 表格完全一致 |

## T6.3 分類色穩定性

| # | 情境 | 期望 |
|---|---|---|
| T6.3.1 | 同一分類在本週彙總為第 1 名、在本月彙總為第 3 名 | 兩次取得的顏色相同 |
| T6.3.2 | `summarizeByCategory` 的回傳 | 不含任何顏色資訊（顏色由分類本身提供，彙總結果不決定顏色） |
| T6.3.3 | `categoryId == null`（未分類） | 顏色為 `#7A7A80` |

（網頁版另外有一組 T7.7「v1→v2 migration 補色」的測案——那是因為網頁版分類色是
**後來才加的欄位**，需要 migration 幫舊資料補色。Android 版從 v1 一開始
`Category` 就有 `color` 欄位，`seedDefaults()` 直接帶入，**沒有這個歷史包袱**，
不需要對應的測案。）

---

# T7 — UI 純函式（金額輸入、週分組標題文案）

這兩組是「presentation-state 的純函式」，不碰 Android/Compose API，
可以放在 `domain/` 或一個獨立的、不依賴 Android 的 `ui-utils` module（挑一個，
團隊統一即可，`CLAUDE.md` 不強制規定放哪裡，只強制規定「不得依賴 Android API」）。

## T7.1 金額輸入位數限制

針對記帳頁的輸入純函式（建議命名 `appendDigit` / `deleteDigit`）。

| # | 現有字串 | 動作 | 期望 |
|---|---|---|---|
| T7.1.1 | `""` | 按 `0` | `"0"` |
| T7.1.2 | `"0"` | 按 `5` | `"5"`（去前導 0） |
| T7.1.3 | `"0"` | 按 `00` | `"0"`（不產生 `"000"`） |
| T7.1.4 | `"12345678"` | 按 `9` | `"12345678"`（達 8 位上限，忽略） |
| T7.1.5 | `"1234567"` | 按 `00` | `"1234567"`（會超過 8 位，整個忽略） |
| T7.1.6 | `"180"` | 按 `⌫` | `"18"` |
| T7.1.7 | `""` | 按 `⌫` | `""` |

## T7.2 週分組標題文案

| # | 週起始日 | referenceDate | 期望標題 |
|---|---|---|---|
| T7.2.1 | `2026-08-31` | `2026-09-03` | `本週 · 8/31–9/6` |
| T7.2.2 | `2026-08-24` | `2026-09-03` | `上週 · 8/24–8/30` |
| T7.2.3 | `2026-08-17` | `2026-09-03` | `8/17–8/23` |
| T7.2.4 | `2025-12-29` | `2026-01-02` | `本週 · 12/29–1/4`（跨年不出現年份） |

任何情況下輸出都不得包含 `YYYY-MM-DD` 格式。

---

# E2E — 使用者行為規格

> 以 Given/When/Then 撰寫，**與執行工具無關**——這批規格是從網頁版 `TESTCASES.md`
> 原封移植的，網頁版用 Playwright 執行，這裡改用 Maestro，**規格文字本身不變**。
> 每個 Given/When/Then 對應一個 `.maestro/*.yaml` flow。

## E2E-1 首次使用

```
Given 使用者第一次開啟 app（無任何資料）
When  畫面載入
Then  顯示引導畫面，要求建立第一個錢包

When  輸入名稱「日常」、幣別「TWD」、預算模式「每週」、金額「3000」
And   送出
Then  進入主畫面
And   顯示錢包「日常」
And   顯示本週餘額「NT$3,000.00」
And   分類清單已預先填入 7 個支出分類與 4 個收入分類
```

## E2E-2 記一筆帳

```
Given 已有錢包「日常」（TWD，週預算 3000）且本週無交易
When  點擊新增按鈕
And   輸入金額「120」、選擇分類「飲食」、日期保持預設（今天）
And   送出
Then  交易出現在列表最上方，顯示「NT$120.00」與「🍜 飲食」
And   本週餘額立即變為「NT$2,880.00」（**無需重新整理**）

When  將該筆金額編輯為「200」
Then  本週餘額立即變為「NT$2,800.00」

When  刪除該筆交易
Then  交易從列表消失
And   本週餘額立即回到「NT$3,000.00」
```

## E2E-3 超支警示

```
Given 錢包「日常」週預算 3000，本週已支出 2900
When  新增一筆支出 500
Then  本週餘額顯示「-NT$400.00」
And   餘額以警示樣式呈現（警示色 + 圖示）
And   畫面明確標示「已超支」
```

## E2E-4 多錢包與多幣別

```
Given 已有錢包「日常」（TWD，週預算 3000，本週已支出 500）
When  建立第二個錢包「日本旅遊」，幣別「JPY」，預算模式「總預算」，金額「200000」
And   切換至「日本旅遊」
Then  顯示剩餘總預算「¥200,000」（**無小數點**）
And   交易列表為空

When  在「日本旅遊」新增支出「¥8,000」分類「飲食」
Then  剩餘總預算顯示「¥192,000」，已用「4%」

When  切換回「日常」
Then  本週餘額仍為「NT$2,500.00」（**完全不受 JPY 交易影響**）
And   畫面任何位置皆不出現跨幣別的合計數字
```

## E2E-5 週起始日設定

```
Given weekStartDay 為「週一」，今天是 2026-08-11（週二）
And   在 2026-08-09（週日）有一筆支出 1000
And   在 2026-08-10（週一）有一筆支出 500
Then  本週餘額為「NT$2,500.00」（只計週一那筆）

When  將 weekStartDay 改為「週日」
Then  本週餘額立即變為「NT$1,500.00」（兩筆都計入）
And   交易列表的週分組標題同步更新
And   **沒有任何交易資料被修改**（金額與日期不變）
```

## E2E-6 備份與還原

```
Given 已有 2 個錢包與 15 筆交易
When  在設定中點擊「匯出備份」
Then  透過 Storage Access Framework 選擇存放位置後成功寫出一個 JSON 檔

Given 清除 app 資料（模擬換裝置，可用 `adb shell pm clear` 或解除安裝重裝模擬）
When  開啟 app 並匯入該備份檔（replace 模式）
Then  2 個錢包與 15 筆交易完整還原
And   所有餘額計算結果與匯出前完全一致
And   週起始日等設定一併還原
```

## E2E-7 離線可用

```
Given app 已安裝並開啟過一次，且已有資料
When  裝置切換為飛航模式
And   重新開啟 app（強制關閉後重開，模擬程序被系統殺掉後回復）
Then  app 正常載入
And   所有既有資料可讀取
And   可正常新增交易
```

（網頁版 E2E-7 後半段是「瀏覽器提供加到主畫面選項」——這是 PWA 特有的驗收項目，
原生 App 天生就是「已安裝」的狀態，不需要對應的測案。這條差異記錄在這裡，
避免後續有人以為漏測了什麼。）

## E2E-8 分類管理：新增分類、系統預設分類保護

```
Given 已有錢包「日常」（TWD，週預算 3000）
When  進入分類管理頁
Then  顯示 7 個支出分類與 4 個收入分類，皆標示「系統預設」

When  新增分類「咖啡」（支出，icon「☕」）
Then  分類清單新增一筆「☕ 咖啡」，不標示「系統預設」

When  嘗試刪除系統預設分類「飲食」
Then  刪除被拒絕（顯示錯誤訊息）
And   「🍜 飲食」仍存在於分類清單
```

## E2E-9 分類管理：刪除有交易的自訂分類，交易轉移到未分類

```
Given 已有錢包「日常」，且已新增自訂分類「咖啡」
And   有一筆分類為「咖啡」的交易
When  刪除分類「咖啡」
Then  分類從清單消失
And   原本該筆交易的分類顯示變為「未分類」
And   交易本身（金額、日期）不遺失
```

## E2E-10 統計：本週／本月分類支出佔比與近 8 週趨勢

```
Given 已有錢包「日常」，weekStartDay 為「週一」，今天是 2026-08-11（週二）
And   本週（週一）有支出「飲食 300」、「交通 200」
And   上週（同月）有支出「飲食 100」
When  進入統計頁（預設顯示「本週」）
Then  分類佔比顯示「飲食 NT$300.00（60%）」與「交通 NT$200.00（40%）」

When  切換為「本月」
Then  分類佔比顯示「飲食 NT$400.00（66.67%）」與「交通 NT$200.00（33.33%）」（本月加總跨週）

Then  近 8 週趨勢長條圖顯示 8 筆資料
And   最後一筆（本週）金額為 500 元、倒數第二筆（上週）金額為 100 元
```

---

# T8 — 進階互動行為（e2e / UI 契約）

對應 `UI-SPEC.md` 的介面契約，而不是 `SPEC.md` 的資料/業務規則，故與上面的
E2E-1～E2E-10 分開編號（沿用網頁版的編號習慣）。

## T8.1 記帳流程

| # | 情境 | 期望 |
|---|---|---|
| T8.1.1 | 點底部導覽中央 `+` | 進入記帳頁，底部導覽不可見 |
| T8.1.2 | 依序點 `1` `8` `0` | 金額顯示 `NT$180` |
| T8.1.3 | 點分類網格的「飲食」 | 該格出現選取態，畫面上不存在任何 `DropdownMenu` |
| T8.1.4 | 點「記一筆」 | 回到首頁，列表出現該筆，Snackbar 顯示「已記錄 飲食 NT$180」 |
| T8.1.5 | 金額為 0 時點「記一筆」 | 按鈕為 disabled 狀態，不送出、不跳頁 |
| T8.1.6 | 點「昨天」後送出 | 交易日期為 referenceDate 的前一天 |
| T8.1.7 | 記帳頁全程 | 系統輸入法（IME）不曾被呼叫出來（自製數字鍵台，不是 `TextField` 取得焦點） |

## T8.2 交易列表互動

| # | 情境 | 期望 |
|---|---|---|
| T8.2.1 | 列表列的畫面 | 不存在文字為「編輯」或「刪除」的可點元素 |
| T8.2.2 | 點某一列 | 導向編輯交易頁 |
| T8.2.3 | 在某列上左滑（`SwipeToDismissBox`） | 該列露出刪除鍵 |
| T8.2.4 | 輕滑一小段後放開（未達刪除閾值） | 該列歸位，刪除鍵不可見 |
| T8.2.5 | 展開 A 列後左滑 B 列 | A 列自動歸位（同時只有一列展開） |
| T8.2.6 | 點刪除鍵 | 該筆消失，Snackbar 出現「還原」動作鍵 |
| T8.2.7 | 點「還原」 | 該筆回到列表，金額與分類不變 |
| T8.2.8 | 週分組標題文字 | 不符合 `\d{4}-\d{2}-\d{2}` 格式 |

## T8.3 導覽與圖表

| # | 情境 | 期望 |
|---|---|---|
| T8.3.1 | 首頁標題列 | 不存在任何模擬連結樣式的純文字按鈕 |
| T8.3.2 | 點錢包名稱 | 出現錢包切換 `ModalBottomSheet`，列出所有未封存錢包與各自餘額 |
| T8.3.3 | 在 sheet 中切換到另一個錢包 | 標題、預算卡金額、交易列表、幣別符號全部更新 |
| T8.3.4 | 統計頁圓環 | 中心可見期間總額文字 |
| T8.3.5 | 統計頁趨勢圖 | 8 個週別標籤都有非零寬度，且每個標籤中心的 x 座標與對應柱子中心的 x 座標差距 < 4dp |
| T8.3.6 | 切到本月再切回本週 | 同一分類的圖例顏色不變 |
| T8.3.7 | 所有可點元素 | 寬與高皆 ≥ 48dp（見 `UI-SPEC.md` §1.2，Android 用 48 不是網頁版的 44） |

---

# 測試品質門檻（CI 強制）

| 項目 | 門檻 | 失敗處理 |
|---|---|---|
| domain 層行覆蓋率（JaCoCo） | ≥ 90% | build 失敗 |
| domain 層 import Android/Compose API | 0 | build 失敗 |
| ktlint | 0 違規 | build 失敗 |
| detekt | 0 違規 | build 失敗 |
| 本文件所有 domain 測案 | 全數通過 | build 失敗 |
| Maestro flow（實機或 emulator） | 全數通過 | build 失敗 |

---

# 給 Agent 的測試撰寫規則

1. **測案編號必須出現在測試名稱中**，用 JUnit 5 的 `@DisplayName("T2.1.4 — 週日應歸屬前一週")`
   或直接把編號放進函式名稱（Kotlin 允許反引號函式名稱，可以寫
   `` fun `T2 1 4 週日應歸屬前一週`() `` 這種形式，兩種寫法擇一，團隊統一即可）。
   這樣 CI 報告能直接對回本文件。
2. Domain 測試**不得** mock 任何東西 — 純函式無須 mock。若需要 mock，代表設計違反了 P1。
3. 時間相關測試必須注入固定的 `referenceDate`（`LocalDate` 或 `Instant`），
   **禁止依賴 `Clock.System.now()`**。
4. Persistence 測試必須從乾淨的 Room in-memory database 開始，每個測試自行建立前置資料。
5. Maestro flow 測試必須從乾淨的 app 狀態開始（`clearState: true`），每個 flow 自行建立前置資料。
6. 新增本文件未涵蓋的測案是**允許且鼓勵的**，但不得刪除或弱化既有測案。
   若認為某測案有誤，在 PR 中提出並暫停，不得自行修改。

package com.jojomango.expensetracker.domain

/**
 * Placeholder proving the `domain` package exists and compiles with zero
 * Android/Compose/Room dependencies (SPEC.md P1). Real domain types
 * (Money, Currency, Week, Month, ...) land starting Phase 1.
 */
internal object DomainMarker {
    const val PHASE: Int = 0
}
